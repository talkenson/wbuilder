{
  "type": "module",
  "scripts": {
    "build": "esbuild ./src/main.ts --outfile=main.js --bundle --tree-shaking=false --format=esm ; cat main.js | pbcopy"
  },
  "dependencies": {
    "esbuild": "^0.17.18",
    "nanoid": "^4.0.2"
  }
}
